---
tags:
  - 모음집
  - 자유메모
---

## 📋 자유롭게 모음

> [!info] 이 노트 정보
> 데일리 노트의 "자유롭게 → 제목 없음" 칸에 작성한 모든 내용을 날짜별로 모아서 보여줍니다.

---

```dataviewjs
const pages = dv.pages('"b. Dairy/1. Daily"')
  .sort(p => p.file.name, 'desc');

for (let page of pages) {
  const content = await dv.io.load(page.file.path);
  const lines = content.split('\n');

  let inSection5 = false;
  let sectionContent = [];

  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];

    // "제목 없음" 콜아웃 시작 감지
    if (line.includes('[!section5]') && line.includes('제목 없음')) {
      inSection5 = true;
      continue;
    }

    // 콜아웃 내용 수집
    if (inSection5) {
      // 다른 콜아웃 시작 감지 (diary, reflection 등)
      if (line.includes('[!') && !line.includes('[!section5]')) {
        break;
      }

      if (line.startsWith('>')) {
        const text = line.replace(/^>\s?/, '');
        sectionContent.push(text);
      } else if (line.trim() === '' && sectionContent.length > 0) {
        // 빈 줄이지만 아직 콜아웃 안
        continue;
      } else if (!line.startsWith('>')) {
        // 콜아웃 끝
        break;
      }
    }
  }

  // 내용이 있으면 출력
  const cleanContent = sectionContent.filter(l => l.trim().length > 0);
  if (cleanContent.length > 0) {
    dv.header(3, `📅 ${page.file.name}`);
    dv.paragraph(cleanContent.join('\n'));
    dv.paragraph('---');
  }
}
```
